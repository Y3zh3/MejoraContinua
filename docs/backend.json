{
  "entities": {
    "ActividadAnual": {
      "title": "Actividad Anual",
      "description": "Datos anuales agregados para una actividad operativa.",
      "type": "object",
      "properties": {
        "nombre": {
          "type": "string",
          "description": "Nombre de la actividad (e.g., Persuasivas, Toma de Estado)."
        },
        "promedioAnual": {
          "type": "number",
          "description": "Promedio anual de cumplimiento para la actividad."
        },
        "metaAnual": {
          "type": "number",
          "description": "La meta anual para la actividad."
        },
        "historicoMensual": {
          "type": "array",
          "description": "Evolución mensual del indicador principal de la actividad.",
          "items": {
            "type": "object",
            "properties": {
              "mes": { "type": "string" },
              "valor": { "type": "number" },
              "meta": { "type": "number" }
            },
            "required": ["mes", "valor"]
          }
        },
        "detalleSedes": {
          "type": "array",
          "description": "Desglose del valor acumulado por sede.",
          "items": {
            "type": "object",
            "properties": {
              "sede": { "type": "string" },
              "valor": { "type": "number" }
            },
            "required": ["sede", "valor"]
          }
        }
      },
      "required": ["nombre", "promedioAnual", "metaAnual", "historicoMensual", "detalleSedes"]
    }
  },
  "auth": {
    "providers": [
      "email"
    ]
  },
  "firestore": {
    "/DashboardAnual/{actividadId}": {
      "schema": {
        "$ref": "#/backend/entities/ActividadAnual"
      },
      "description": "Colección para los datos anuales de las actividades del dashboard."
    }
  }
}
